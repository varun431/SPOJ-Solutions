#include <bits/stdc++.h>

int main()
{
    int n,q,a,b,op,i;
    scanf("%d %d",&n,&q);
    int a[n];
    while(q--)
    {
        scanf("%d %d %d",&op,&a,&b);
        for(i=a;i<b;i++)
        {
            if(op==1)
            {

            }
        }
    }
}
